[project]
name = "transformer-from-scratch"
version = "0.1.0"
description = "Transformer from scratch w/o pytorch"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.2.5",
]
